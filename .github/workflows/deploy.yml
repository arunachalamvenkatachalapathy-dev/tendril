name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build-and-deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        working-directory: ./frontend
        run: bun install --frozen-lockfile || bun install

      - name: Build frontend
        working-directory: ./frontend
        env:
          VITE_API_BASE_URL: https://tendril-api-j22ux7ph4a-el.a.run.app
          VITE_FIREBASE_API_KEY: AIzaSyBqacPskeWDEspm3tKkJqugYd13fvKSVaM
          VITE_FIREBASE_AUTH_DOMAIN: tendril-74291.firebaseapp.com
          VITE_FIREBASE_PROJECT_ID: tendril-74291
          VITE_FIREBASE_STORAGE_BUCKET: tendril-74291.firebasestorage.app
          VITE_FIREBASE_MESSAGING_SENDER_ID: "1007307057399"
          VITE_FIREBASE_APP_ID: "1:1007307057399:web:8e66de9570c24d3d380bf2"
        run: |
          bun run build
          cp dist/index.html dist/404.html

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./frontend/dist

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
